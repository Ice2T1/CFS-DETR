diff --git a/ultralytics/nn/tasks.py b/ultralytics/nn/tasks.py
--- a/ultralytics/nn/tasks.py
+++ b/ultralytics/nn/tasks.py
@@
 from ultralytics.nn.modules import *
+from cfs_detr import CrossScaleInvariantModule, FSENetBlock
@@
     def init_criterion(self):
         from ultralytics.models.utils.loss import RTDETRDetectionLoss
-        return RTDETRDetectionLoss(nc=self.nc, use_vfl=True)
+        return RTDETRDetectionLoss(nc=self.nc, use_vfl=True,
+                                   domain_loss_weight=self.yaml.get('domain_loss_weight', 1.5))
@@
         targets = {
             'cls': batch['cls'].to(img.device, dtype=torch.long).view(-1),
             'bboxes': batch['bboxes'].to(device=img.device),
             'batch_idx': batch_idx.to(img.device, dtype=torch.long).view(-1),
             'gt_groups': gt_groups}
 
-        preds = self.predict(img, batch=targets) if preds is None else preds
-        dec_bboxes, dec_scores, enc_bboxes, enc_scores, dn_meta = preds if self.training else preds[1]
+        domain_labels = batch.get('domain_labels')
+        if domain_labels is not None:
+            domain_labels = domain_labels.to(device=img.device, dtype=torch.long)
+        preds = self.predict(img, batch=targets,
+                             domain_epoch=batch.get('domain_epoch', 0)) if preds is None else preds
+        domain_logits = None
+        if self.training and isinstance(preds, tuple) and len(preds) == 2:
+            head_output, domain_logits = preds
+        else:
+            head_output = preds
+        dec_bboxes, dec_scores, enc_bboxes, enc_scores, dn_meta = \
+            head_output if self.training else head_output[1]
@@
         loss = self.criterion((dec_bboxes, dec_scores), targets,
                               dn_bboxes=dn_bboxes, dn_scores=dn_scores,
-                              dn_meta=dn_meta)
-        return sum(loss.values()), torch.as_tensor(
-            [loss[k].detach() for k in ['loss_giou', 'loss_class', 'loss_bbox']], device=img.device)
+                              dn_meta=dn_meta, domain_logits=domain_logits,
+                              domain_labels=domain_labels)
+        return sum(loss.values()), torch.as_tensor(
+            [loss[k].detach() for k in ['loss_giou', 'loss_class', 'loss_bbox', 'loss_domain']],
+            device=img.device)
@@
-    def predict(self, x, profile=False, visualize=False, batch=None, augment=False):
+    def predict(self, x, profile=False, visualize=False, batch=None, augment=False, domain_epoch=0):
         y, dt = [], []
+        domain_logits = None
         for m in self.model[:-1]:
@@
             else:
-                x = m(x)
+                if isinstance(m, CrossScaleInvariantModule):
+                    if self.training:
+                        domain_logits = m(x, epoch=domain_epoch)
+                else:
+                    x = m(x)
                 y.append(x if m.i in self.save else None)
@@
         head = self.model[-1]
         x = head([y[j] for j in head.f], batch)
+        if self.training and domain_logits is not None:
+            return x, domain_logits
         return x

diff --git a/ultralytics/models/utils/loss.py b/ultralytics/models/utils/loss.py
--- a/ultralytics/models/utils/loss.py
+++ b/ultralytics/models/utils/loss.py
@@
 class RTDETRDetectionLoss(DETRLoss):
+    def __init__(self, *args, domain_loss_weight=1.5, **kwargs):
+        super().__init__(*args, **kwargs)
+        self.domain_loss_weight = float(domain_loss_weight)
+
-    def forward(self, preds, batch, dn_bboxes=None, dn_scores=None, dn_meta=None):
+    def forward(self, preds, batch, dn_bboxes=None, dn_scores=None, dn_meta=None,
+                domain_logits=None, domain_labels=None):
@@
         else:
             total_loss.update({f'{k}_dn': torch.tensor(0., device=self.device)
                                for k in total_loss.keys()})
+        if domain_logits is not None and domain_labels is not None:
+            domain_labels = domain_labels.to(device=domain_logits.device,
+                                             dtype=torch.long).view(-1)
+            domain_loss = F.cross_entropy(domain_logits, domain_labels)
+            total_loss['loss_domain'] = self.domain_loss_weight * domain_loss
+        else:
+            total_loss['loss_domain'] = torch.tensor(0., device=self.device)
         return total_loss

diff --git a/ultralytics/models/rtdetr/train.py b/ultralytics/models/rtdetr/train.py
--- a/ultralytics/models/rtdetr/train.py
+++ b/ultralytics/models/rtdetr/train.py
@@
 import torch
+from cfs_detr import generate_domain_labels
@@
-        self.loss_names = 'giou_loss', 'cls_loss', 'l1_loss'
+        self.loss_names = 'giou_loss', 'cls_loss', 'l1_loss', 'domain_loss'
@@
         batch = super().preprocess_batch(batch)
+        image_paths = batch.get('im_file', batch.get('img_path'))
+        if image_paths is None:
+            raise KeyError('Image paths are required to generate scale-domain labels.')
+        batch['domain_labels'] = generate_domain_labels(image_paths)
+        batch['domain_epoch'] = self.epoch
         bs = len(batch['img'])
